import { useContext } from "react";
import { recipesProvider } from "../context/RecipesContext";

const RecipeCard = () => {

    const { } = useContext(recipesProvider);

    return (
        <>
        </>
    )
}

export default RecipeCard