import '../../App.css';
import { useContext } from "react";
import { UsersProvider } from '../context/UsersContext';

const Register = () => {

  const { } = useContext(UsersProvider);

  return (
    <>
    </>
  )
}

export default Register
